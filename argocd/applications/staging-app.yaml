apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: simple-python-app-staging
  namespace: argocd
  labels:
    app.kubernetes.io/name: simple-python-app
    app.kubernetes.io/instance: staging
    app.kubernetes.io/part-of: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/BaruchiHalamish20/simple-python-app-devops
    targetRevision: HEAD
    path: environments/staging
  destination:
    server: https://kubernetes.default.svc
    namespace: simple-python-app-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  revisionHistoryLimit: 10
  info:
    - name: "Environment"
      value: "Staging"
    - name: "Namespace"
      value: "simple-python-app-staging"

